"use client";

// Den projektbaserede chat-widget er erstattet af kontraktspecifik chat.
// Chat åbnes via /projekt/[id]/chat og /projekt/[id]/chat/[kontraktId].
export default function Chat(_props: { bruger?: string; projektId?: string }) {
  return null;
}
